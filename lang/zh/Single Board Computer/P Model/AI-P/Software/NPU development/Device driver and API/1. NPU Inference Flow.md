# 1. NPU 推理流程

<p align="center"><img src="https://raw.githubusercontent.com/topst-development/Documentation/refs/heads/main/Single%20Board%20Computer/P%20Model/AI-P/Software/media/1.%20NPU%20Inference%20Flow.image1.png?raw=true"></p>

<p align="center"><strong>图 1.1 NPU 推理流程</strong>


<table>
<colgroup>
<col style="width: 28%" />
<col style="width: 71%" />
</colgroup>
<tbody>
<tr class="odd">
<td><strong>步骤说明</strong></td>
<td><strong>步骤说明</strong></td>
</tr>
<tr class="even">
<td>Open</td>
<td>调用 npu_open 以生成 NPU Device Handle。</td>
</tr>
<tr class="odd">
<td>更新 MLX 固件</td>
<td>通过 npu_init_mlx 加载 MLX Kernel 固件并启用 MLX。</td>
</tr>
<tr class="even">
<td>创建 Network</td>
<td>使用 npu_load_network 创建 network handle。</td>
</tr>
<tr class="odd">
<td>创建 Input Buffer</td>
<td>通过 buffer_alloc 创建 Input buffer。</td>
</tr>
<tr class="even">
<td>创建 Output Buffer</td>
<td>通过 buffer_alloc 创建 Onput buffer。</td>
</tr>
<tr class="odd">
<td>读取输入图像</td>
<td><p>通过 buffer_get_addr 获取输入缓冲区地址。</p>
<p>将图像加载到输入缓冲区中。</p>
<p>原始图像数据可能需要进行缩放等处理，具体
取决于</p>
<p>神经网络输入图像尺寸和颜色格式。</p></td>
</tr>
<tr class="even">
<td>运行 Network 推理</td>
<td><p>运行 network_issue_run 以向 HW 发出 RUN_INFERENCE
命令。</p>
<p>运行 network_wait_done 以等待 RUN_INFERENCE 命令
结束。</p></td>
</tr>
<tr class="odd">
<td>读取输出 Tensor</td>
<td>从 buffer_get_addr 获取输出缓冲区地址</td>
</tr>
<tr class="even">
<td>运行后处理</td>
<td>后处理 (如有必要)</td>
</tr>
</tbody>
</table>

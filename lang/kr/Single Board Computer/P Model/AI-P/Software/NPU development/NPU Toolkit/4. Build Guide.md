# 4. 빌드 가이드

<br/><br/>

## 4.1 컨버터

컨버터는 신경망 파일을 읽어 TOPST AI NPU SW Toolkit에서 사용하는
네트워크 파일 형식인 ”bin” 파일로 변환합니다.
또한 네트워크 추론을 수행하여 이후 네트워크 양자화에 사용되는
활성화 데이터의 통계를 수집합니다.

<br/>

<strong>1.  Darknet</strong>

```
$ python ./EnlightSDK/converter.py \
  ./input_networks/yolov4.cfg \
  --weight ./input_networks/yolov4.weights \
  --type obj \
  --dataset Custom \
  --dataset-root my_dataset_path \
  --output ./output_networks/yolov4.enlight \
  --enable-track \
  --mean 0 0 0 \
  --std 1 1 1 \
  --num-class 90
```

<br/>

<table>
<colgroup>
<col style="width: 47%" />
<col style="width: 52%" />
</colgroup>
<tbody>
<tr class="odd">
<td>./input_networks/yolov4.cfg</td>
<td>입력 네트워크 파일 이름</td>
</tr>
<tr class="even">
<td>--weight ./input_networks/yolov4.weights</td>
<td>Darknet 가중치 파일 이름</td>
</tr>
<tr class="odd">
<td>--type obj</td>
<td><p>후처리 유형:</p>
<p>분류 (class),</p>
<p>객체 검출 (obj),</p>
<p>또는 알 수 없음 (unknown)</p></td>
</tr>
<tr class="even">
<td>--dataset Custom</td>
<td>Custom: 사용자 정의 데이터셋</td>
</tr>
<tr class="odd">
<td>--dataset-root ./my_dataset_path</td>
<td><p>활성화 통계 수집에 사용되는</p>
<p>입력 데이터의 경로</p></td>
</tr>
<tr class="even">
<td>--output ./output_networks/yolov4.enlight</td>
<td>컨버터 출력 파일 이름</td>
</tr>
<tr class="odd">
<td>--enable-track</td>
<td>활성화 통계 수집을 활성화합니다</td>
</tr>
<tr class="even">
<td>--mean 0 0 0</td>
<td>입력 정규화의 평균값 (RGB)</td>
</tr>
<tr class="odd">
<td>--std 1 1 1</td>
<td>입력 정규화의 표준편차값 (RGB)</td>
</tr>
<tr class="even">
<td>--num-class 90</td>
<td>객체 클래스 개수</td>
</tr>
</tbody>
</table>

<br/>

<strong>2.  TensorFlow Lite</strong>

```
$ python ./EnlightSDK/converter.py \
  ./input_networks/lite-model_ssd_mobilenet_v1_100_320_fp32_nms_1.tflite \
  --type obj \
  --dataset Custom \
  --dataset-root my_dataset_path \
  --output ./output_networks/lite-model_ssd_mobilenet_v1_100_320_fp32_nms_1.enlight \
  --enable-track \
  --mean 0.5 0.5 0.5 \
  --std 0.5 0.5 0.5 \
  --num-class 90
```

<br/>

<table>
<colgroup>
<col style="width: 58%" />
<col style="width: 41%" />
</colgroup>
<tbody>
<tr class="odd">
<td>./input_networks/lite-model_ssd_mobilenet_v1_100_320_fp32_nms_1.tflite</td>
<td>입력 네트워크 파일 이름</td>
</tr>
<tr class="even">
<td>--type obj</td>
<td><p>후처리 유형:</p>
<p>분류 (class),</p>
<p>객체 검출 (obj),</p>
<p>또는 알 수 없음 (unknown)</p></td>
</tr>
<tr class="odd">
<td>--dataset Custom</td>
<td>Custom: 사용자 정의 데이터셋</td>
</tr>
<tr class="even">
<td>--dataset-root ./my_dataset_path</td>
<td><p>활성화 통계 수집에 사용되는</p>
<p>입력 데이터의 경로</p></td>
</tr>
<tr class="odd">
<td>--output
./output_networks/lite-model_ssd_mobilenet_v1_100_320_fp32_nms_1.enlight</td>
<td>컨버터 출력 파일 이름</td>
</tr>
<tr class="even">
<td>--enable-track</td>
<td>활성화 통계 수집을 활성화합니다</td>
</tr>
<tr class="odd">
<td>--mean 0.5 0.5 0.5</td>
<td>입력 정규화의 평균값 (RGB)</td>
</tr>
<tr class="even">
<td>--std 0.5 0.5 0.5</td>
<td>입력 정규화의 표준편차값 (RGB)</td>
</tr>
<tr class="odd">
<td>--num-class 90</td>
<td>객체 클래스 개수</td>
</tr>
</tbody>
</table>

<br/>

<strong>3.  ONNX</strong>

```
$ python ./EnlightSDK/converter.py
  ./input_networks/mb2-ssd-lite.onnx \
  --type obj \
  --add-detection-post-process ./input_networks/mb2-ssd-lite.anchor \
  --logistic softmax \
  --dataset Custom \
  --dataset-root my_dataset_path \
  --output ./output_networks/mb2-ssd-lite.enlight \
  --enable-track \
  --mean 0.498 0.498 0.498 \
  --std 0.502 0.502 0.502 \
  --num-class 20 \
  --variance 0.1 0.2
```

<br/>

<table>
<colgroup>
<col style="width: 58%" />
<col style="width: 41%" />
</colgroup>
<tbody>
<tr class="odd">
<td>./input_networks/mb2-ssd-lite.onnx</td>
<td>입력 네트워크 파일 이름</td>
</tr>
<tr class="even">
<td>--type obj</td>
<td><p>후처리 유형:</p>
<p>분류 (class),</p>
<p>객체 검출 (obj),</p>
<p>또는 알 수 없음 (unknown)</p></td>
</tr>
<tr class="odd">
<td>--add-detection-post-process
./input_networks/mb2-ssd-lite.anchor</td>
<td>기본 박스 파일 이름</td>
</tr>
<tr class="even">
<td>--logistic softmax</td>
<td><p>후처리의 로지스틱 함수:</p>
<p>softmax, sigmoid, none</p></td>
</tr>
<tr class="odd">
<td>--dataset Custom</td>
<td>Custom: 사용자 정의 데이터셋</td>
</tr>
<tr class="even">
<td>--dataset-root ./my_dataset_path</td>
<td><p>활성화 통계 수집에 사용되는</p>
<p>입력 데이터의 경로</p></td>
</tr>
<tr class="odd">
<td>--output ./output_networks/mb2-ssd-lite.enlight</td>
<td>컨버터 출력 파일 이름</td>
</tr>
<tr class="even">
<td>--enable-track</td>
<td>활성화 통계 수집을 활성화합니다</td>
</tr>
<tr class="odd">
<td>--mean 0.498 0.498 0.498</td>
<td>입력 정규화의 평균값 (RGB)</td>
</tr>
<tr class="even">
<td>--std 0.502 0.502 0.502</td>
<td>입력 정규화의 표준편차값 (RGB)</td>
</tr>
<tr class="odd">
<td>--num-class 20</td>
<td>객체 클래스 개수</td>
</tr>
<tr class="even">
<td>--variance 0.1 0.2</td>
<td>SSD 검출 레이어의 x 및 y 분산</td>
</tr>
</tbody>
</table>

<br/><br/>

## 4.2 양자화기

양자화기는 부동 소수점 연산 기반의 원본 네트워크 모델을
정수 기반의 (양자화된) 네트워크로 변환합니다.

<br/>

**1.  Darknet**

```
$ python ./EnlightSDK/quantizer.py \
  ./output_networks/yolov4.enlight \
  --output ./output_networks/yolov4_quantized.enlight
```

<br/>

**2.  TensorFlow Lite**

```
$ python ./EnlightSDK/quantizer.py \
  ./output_networks/lite-model_ssd_mobilenet_v1_100_320_fp32_nms_1.enlight \
  --output ./output_networks/lite-model_ssd_mobilenet_v1_100_320_fp32_nms_1_quantized.enlight
```

<br/>

**3.  ONNX**

```
$ python ./EnlightSDK/quantizer.py \
  ./output_networks/mb2-ssd-lite.enlight \
  --output ./output_networks/mb2-ssd-lite_quantized.enlight
```

<br/><br/>

## 4.3 컴파일러

컴파일러는 양자화된 신경망을 TOPST NPU HW에서 신경망을 처리하기 위한
NPU 코드 및 네트워크 파라미터로 컴파일합니다.

<br/>

**1.  Darknet**

```
$ python ./EnlightSDK/compiler.py \
  ./output_networks/yolov4_quantized.enlight \
  --th-iou 0.5 \
  --th-conf 0.5
```

<br/>

**2.  TensorFlow Lite**

```
$ python ./EnlightSDK/compiler.py \
  ./output_networks/lite-model_ssd_mobilenet_v1_100_320_fp32_nms_1_quantized.enlight \
  --th-iou 0.5 \
  --th-conf 0.5
```

<br/>

**3.  ONNX**

```
$ python ./EnlightSDK/compiler.py \
  ./output_networks/mb2-ssd-lite_quantized.enlight \
  --th-iou 0.5 \
  --th-conf 0.5
```

<br/>

<table>
<colgroup>
<col style="width: 51%" />
<col style="width: 48%" />
</colgroup>
<tbody>
<tr class="odd">
<td>--th-iou 0.5</td>
<td><p>IOU 임계값.</p>
<p>검출 신경망에만 유효합니다.</p></td>
</tr>
<tr class="even">
<td>--th-conf 0.5</td>
<td><p>신뢰도 임계값.</p>
<p>검출 신경망에만 유효합니다.</p></td>
</tr>
</tbody>
</table>

<br/>

생성된 파일은 “output_code/\<network_name\>” 디렉터리에
저장됩니다.

컴파일러 출력 파일:

- npu_cmd.bin: TELECHIPS NPU 추론 코드
- quantized_network.bin: 네트워크 가중치 및 바이어스 파라미터
- network.h: 후처리를 위한 버퍼 크기 및 네트워크 파라미터
- post_process.c: 후처리 코드
- 기타 파일: 디버깅 정보 (내부용)

신경망 애플리케이션에서 “npu_cmd.bin”과 “quantized_network.bin”은
TELECHIPS NPU API와 Linux 디바이스 드라이버를 사용하여 DMA 버퍼에
로드됩니다. “network.h”와 “post_process.c”는 Network Object Build에 나타난 것과 같이
네트워크 오브젝트 파일 (.so)로 빌드됩니다.
